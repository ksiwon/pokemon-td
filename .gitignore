node_modules
dist
dist-ssr
*.local
.vscode
.DS_Store
.env
.claude/settings.local.json

# PGlite 디버그 로그 (실수 커밋 방지)
pglite-debug.log

# Firebase 서비스 계정 키 (DB 전체 권한 — 절대 커밋 금지)
serviceAccount.json
*serviceAccount*.json
*-firebase-adminsdk-*.json
# 밸런스 시뮬 하네스 산출물 (재생성 가능)
sim/fixtures/http-cache/
sim/reports/current/

# Firebase 에뮬레이터 로그(rules:check 실행 시 생김)
firebase-debug.log
database-debug.log
firestore-debug.log
ui-debug.log

# 화면 캡처 산출물 (npm run shots — 재생성 가능)
shots/

# 캡처 도구가 쓰는 브라우저 프로필(게스트 세션 재사용)
.playwright-profile/
